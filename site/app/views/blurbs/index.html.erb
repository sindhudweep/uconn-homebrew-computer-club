<h1>Listing blurbs</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>

<% for blurb in @blurbs %>
  <tr>
    <td><%=h blurb.title %></td>
    <td><%=h blurb.body %></td>
    <td><%= link_to 'Show', blurb %></td>
    <td><%= link_to 'Edit', edit_blurb_path(blurb) %></td>
    <td><%= link_to 'Destroy', blurb, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New blurb', new_blurb_path %>
